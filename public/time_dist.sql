create function public.time_dist(time, time) returns interval
	immutable
	strict
	language c
as $$
begin
-- missing source code
end;
$$;

alter function public.time_dist(time, time) owner to streaming_user;

